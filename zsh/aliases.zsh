# ls aliases
alias ll='ls -la'
alias la='ls -la'
alias l='ls -CF'

# archiving
alias tgz='tar -cvvzf'
alias tbz2='tar -cvvjf'
alias utgz='tar -xvvzf'
alias utbz2='tar -xvvjf'
alias mktar='tar -cvvf'
alias untar='tar -xvvf'

# orientation
alias cls='clear;echo "Logged in at $(tty), as $(whoami) in $(pwd)."'

# ssh with iTerm background
#alias ssh='${LIQUIDZ_DIR}/ssh-iterm'
